import QueryInterface from '../../../../Infrastructure/QueryBus/Query/IQuery';

class {{action}}{{grouping}}Query implements QueryInterface
{
{{class_attributes}}
    public function __construct(
{{constructor_parameters}}
    ) {
{{constructor_assignments}}
    }
{{get_methods}}
}

export default {{action}}{{grouping}}Query;