import CommandInterface from '../../../../Infrastructure/CommandBus/Command/ICommand';

class {{action}}{{grouping}}Command implements CommandInterface
{
{{class_attributes}}
    public constructor(
{{constructor_parameters}}
    ) {
{{constructor_assignments}}
    }
{{get_methods}}
}

export default {{action}}{{grouping}}Command;